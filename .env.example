# Copiez ce fichier en .env pour tester la fonction du Gardien en local
# avec `netlify dev`. Ne committez JAMAIS le fichier .env lui-même
# (déjà exclu par .gitignore) — seule cette version d'exemple, sans
# vraie clé, doit être versionnée.

ANTHROPIC_API_KEY=sk-ant-votre-cle-ici
